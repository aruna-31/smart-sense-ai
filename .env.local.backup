# Gemini key used by the backend (do NOT commit real keys)
GEMINI_API_KEY=
# Optional: If you want the frontend to call backend with full origin, set this. Otherwise the Vite proxy forwards /api to http://localhost:8000
VITE_BACKEND_URL=
# Optional: backend model override
GEMINI_MODEL=gemini-2.5-flash
